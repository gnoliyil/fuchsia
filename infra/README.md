# //infra Purpose

This directory contains configurations used by the Infrastructure/EngProd teams
that are versioned with the Fuchsia platform.

This directory is not intended to contain source code used by
Infrastructure/EngProd, locations in the //tools/... or //src/... directories
are intended to fulfill that purpose.