# Copyright 2022 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/components.gni")
import("//src/proc/bin/starnix/starnix.gni")

fuchsia_package_with_single_component("empty_galaxy") {
  manifest = "meta/empty_galaxy.cml"
  package_name = "starless"
  component_name = "empty_galaxy"

  if (target_cpu == "x64") {
    deps = [ "//src/proc/bin/galaxies:default_init" ]
  }
}
