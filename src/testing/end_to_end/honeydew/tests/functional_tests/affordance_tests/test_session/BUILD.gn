# Copyright 2023 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/python/python_mobly_test.gni")
import("//build/testing/environments.gni")

if (is_host) {
  python_mobly_test("session_test_fc") {
    main_source = "test_session.py"
    libraries = [
      "//src/testing/end_to_end/honeydew",
      "//src/testing/end_to_end/mobly_base_tests:fuchsia_base_test",
    ]
    transport = "fuchsia-controller"
    environments = [ emu_env ]
  }

  # Group containing all of the Session affordance functional test cases.
  group("tests") {
    testonly = true
    public_deps = [ ":session_test_fc($host_toolchain)" ]
  }
}

group("packages") {
  testonly = true
  public_deps = [ "//src/ui/examples:flatland-examples" ]
}

group("test_session") {
  testonly = true
  deps = [
    ":packages",
    ":tests($host_toolchain)",
  ]
}
